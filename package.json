{
  "name": "1of10",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "db:generate": "turbo run db:generate",
    "db:push": "turbo run db:push",
    "db:migrate": "turbo run db:migrate",
    "vercel-build": "pnpm --filter @repo/db db:generate && pnpm --filter @repo/web build",
    "test": "turbo test"
  },
  "devDependencies": {
    "next": "^16.2.1",
    "turbo": "^2"
  },
  "packageManager": "pnpm@9.15.4",
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
